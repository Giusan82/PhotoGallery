<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">
    <item
        android:id="@+id/photo_zoom"
        android:icon="?attr/fullscreen_icon"
        android:title="@string/photo_zoom"
        app:showAsAction="never" />
    <item
        android:id="@+id/photo_download"
        android:icon="?attr/cloud_download_icon"
        android:title="@string/photo_download"
        app:showAsAction="never" />
    <item
        android:id="@+id/like"
        android:icon="?attr/favorite_icon"
        android:title="@string/like"
        app:showAsAction="never" />
</menu>